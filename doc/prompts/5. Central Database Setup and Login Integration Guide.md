<!-- Frontmatter Tags: #prompt #plan #session #login #database -->

# Chat Conversation: 5. Central Database Setup and Login Integration Guide

Note: _This is purely the output of the chat conversation and does not contain any raw data, codebase snippets, etc. used to generate the output._

### User Input

```text
let's proceed with setting up a working login functionality for the website. refer to @[d:\GitHub\[oU1TS]\ou1ts.github.io\doc\idea\[DoNotCommit]oU1TS_Central_Database_Guide(from portal repo).md] . We will keep it simple at first and update the requirements later. For now just update the reference doc with Blood Group information as a variable for profile database.
```

### Thinking

The user wants to proceed with setting up working login functionality for the website referencing the central database guide. For this specific step, the user requests to keep it simple and update the reference document with "Blood Group" information as a variable in the profile database.
I will inspect `[DoNotCommit]oU1TS_Central_Database_Guide(from portal repo).md`, locate the `profiles` table schema in Step 2.1 and the auto-profile trigger in Step 2.3, and add the `blood_group` column and metadata mapping. Then I will confirm the modification to the user.

### AI Response

The reference document [[DoNotCommit]oU1TS_Central_Database_Guide(from portal repo).md](file:///d:/GitHub/%5BoU1TS%5D/ou1ts.github.io/doc/idea/%5BDoNotCommit%5DoU1TS_Central_Database_Guide(from%20portal%20repo).md) has been updated with the `blood_group` variable:

1. **Table Schema (`public.profiles`)**:
   Added `blood_group TEXT` (e.g. `'A+'`, `'A-'`, `'B+'`, `'B-'`, `'AB+'`, `'AB-'`, `'O+'`, `'O-'`).
2. **Trigger Function (`handle_new_user`)**:
   Added `COALESCE(NEW.raw_user_meta_data->>'blood_group', NULL)` to insert the blood group on new user registration.

Whenever you are ready to proceed with the next steps for login implementation or database setup, let me know!

---

### User Input

```text
I am ready, prepare a detailed step by step guide for  me to follow in @[d:\GitHub\[oU1TS]\ou1ts.github.io\doc] . Also prepare and execute "D:\GitHub\[oU1TS]\b1t-Acad\AGENTS.md" for this project structure.
```

### Thinking

The user requested two main actions:
1. Prepare and execute the guidelines from `D:\GitHub\[oU1TS]\b1t-Acad\AGENTS.md` for this project structure (`ou1ts.github.io`).
   - Read `D:\GitHub\[oU1TS]\b1t-Acad\AGENTS.md` to analyze the rules:
     - Section 1: Changelog & History Maintenance (`doc/history.md`) with `# DD.MM.YY` headers.
     - Section 2: Mandatory prompt, plan, and walkthrough archiving (`doc/prompts/<Prefix>. <Session Title>.md`).
     - Section 3: Code quality & styling standards (vanilla JS/CSS, responsive, dark theme).
     - Section 4: Commit message & structured summary generation at the end of every turn.
   - Created `AGENTS.md` in workspace root.
   - Initialized `doc/history.md` with current date `# 18.09.26`.
   - Updated database SQL schemas in `user_profile_schema.sql` and `supabase_setup_guide.md` to synchronize `blood_group`.
   - Created `doc/prompts/5. Central Database Setup and Login Integration Guide.md`.
2. Prepare a detailed, actionable step-by-step guide for the user to follow in `doc/`:
   - Created `doc/step_by_step_login_setup_guide.md` covering Supabase project creation, SQL schema execution, redirect configuration, local `env-config.js` testing, GitHub Actions secrets for deployment, and troubleshooting.

### AI Response

I have prepared the step-by-step guide and adapted and executed the repository rules from `b1t-Acad/AGENTS.md` for this project:

1. **Step-by-Step Login Setup Guide**:
   - Created [`doc/step_by_step_login_setup_guide.md`](doc/step_by_step_login_setup_guide.md) with an end-to-end walkthrough covering:
     - Supabase project creation & key retrieval.
     - Database schema execution (profiles table with `blood_group`, constraints, and triggers).
     - Authentication URL configuration & allowed redirect domains.
     - Local configuration in `env-config.js` and in-browser testing.
     - GitHub Actions secrets setup for GitHub Pages production deployment.
     - Verification & troubleshooting checklist.

2. **Schema Synchronization**:
   - Updated [`doc/db/user_profile_schema.sql`](doc/db/user_profile_schema.sql) and [`doc/db/supabase_setup_guide.md`](doc/db/supabase_setup_guide.md) to ensure the `blood_group` column and trigger insertion logic are completely synchronized across all references.

3. **Repository Rules (`AGENTS.md`)**:
   - Created [`AGENTS.md`](AGENTS.md) at the root of `ou1ts.github.io`, tailored to this project's structure.
   - Enforces changelog maintenance in `doc/history.md`, per-turn prompt archiving in `doc/prompts/`, vanilla JS/CSS code styling guidelines, and commit message suggestions at the end of every response.

4. **Execution & History Maintenance**:
   - Initialized [`doc/history.md`](doc/history.md) under `# 18.09.26`.
   - Created [`doc/prompts/5. Central Database Setup and Login Integration Guide.md`](doc/prompts/5.%20Central%20Database%20Setup%20and%20Login%20Integration%20Guide.md) preserving conversation turns, thinking processes, and responses.
